# Decisions log (active only)

> Short-form ADR. One line per field. Mark `Status: superseded` and move
> the entry into `decisions-archive.md` immediately. If this file passes
> ~20–25 active entries, move the oldest half that no longer constrain
> today's work into `decisions-archive.md` too.

## <YYYY-MM-DD> — <Decision title>
**Context:** <why this came up — one line>
**Decision:** <what was chosen — one line>
**Alternatives:** <rejected options — one line>
**Challenge:** <link to challenge-report if non-obvious; else "tech-choices forced">
**Status:** active
