---
description: Shipjaw continuation contract for this repo — docs-first, INDEX-led, gate before done.
alwaysApply: true
---

# Shipjaw project contract

This app was scaffolded with **Shipjaw**. For features, fixes, refactors, and
resumed work, follow the **`shipjaw-ask`** contract (prefer invoking
`/shipjaw-ask`; if not invoked, still obey this rule).

## Do

1. Read `documentation/INDEX.md` first. Use **Open when** as a hard filter.
2. Open at most 1–2 KB/product files the task needs. Prefer Grep + offset reads.
3. Implement against project tooling (strict TS, eslint, domain → application
   → infrastructure → presentation). Place types/constants/helpers by
   ownership — no grab-bag `lib/utils.ts`.
4. Domain/application changes → Vitest (happy path + required edges).
5. Critical *User can…* journeys → Playwright golden path + e2e edges + axe.
6. Bug fix → regression test (fails before, passes after). Never weaken tests
   to green.
7. Run the gate (typecheck → lint → unit → e2e as relevant). Stop after 2
   failed fix attempts and ask the human.
8. Update `documentation/` surgically before ending (changelog, features-index,
   touched KB, phase status). State lives in the repo, not the chat.

## Don't

- Re-run bootstrap (`shipjaw-build`) or recreate the stack when KB exists.
- Implement Out-of-v1 / out-of-phase scope “while we’re here”.
- Invent business rules when behavior is ambiguous after one clarifying
  question — stop and ask.
- Dump framework docs into `documentation/` or paste large code into docs.
- Park domain types or business constants inside React files or a global utils bag.

One-off CSS/copy typos may skip the full protocol; still don’t break the gate.
